# requirements.txt — Python dependencies for the ML pipeline
# Install all of these with: pip install -r requirements.txt --break-system-packages

scikit-learn==1.5.2   # Machine learning models: Gradient Boosting, Isolation Forest
pandas==2.2.3         # DataFrame library — like a spreadsheet you can code against
numpy==1.26.4         # Numerical arrays; pandas and scikit-learn are built on top of this
openai==1.54.0        # OpenAI API client, for the advice-generation LLM calls (Phase 7)
pytest==8.3.3         # Testing framework for the ML pipeline